[verify]
  phase = "public-config-surface"
  generated_at = 2026-09-09T07:42:34.846237Z
  verdict = "fail"
  finalized = true
  finalized_at = 2026-09-09T07:47:09.202747Z

[summary]
  mutation_status = "skipped"
  measured_on = "local"
  mutation_score = 0.0
  mutants_killed = 0
  mutants_survived = 0
  mutants_not_covered = 0
  mutants_in_scope = 0
  unclassified_in_scope = 0
  criteria_total = 5
  criteria_covered = 3
  criteria_uncovered = 2

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["tests/codriver/config_spec.lua:describe('the bash_allow option') (accepts, deep-validates, defaults nil)", "tests/codriver/hook_bash_spec.lua:describe('bash_allow additions') (heads/git_subcommands extend the allowlist additively, hardcoded defaults survive)", "tests/nvim/hook_state_check.lua (bash_allow round-trips through the JSON state file unchanged, absent reads back nil)", "tests/nvim/hook_publish_check.lua (setup() publishes resolved bash_allow, exposes it on the codriver module, survives a role-flip republish)", "tests/codriver/hook_decision_spec.lua:\"threads session.bash_allow through to the bash matcher\""]
  notes = "The accept/additive-only behavioural claims are thoroughly covered across four layers (validation, matcher merge, state persistence, decision wiring). The 'documented in the README' clause has no automated test — see the NOTE finding; not downgraded because the behavioural substance of the criterion is what the tests exist to protect."

[[criterion]]
  id = "c-2"
  status = "uncovered"
  notes = "Purely a documentation criterion: 'test_command's exact-match exemption is documented in the README.' The exemption behaviour itself was already covered before this phase (hook_bash_spec.lua:\"matches the configured test command whole, never as a prefix\"), but nothing in the suite reads README.md — no test would fail if this phase's README edit were reverted or drifted out of date."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["tests/codriver/config_spec.lua:describe('the bash_allow option') — rejects non-table bash_allow, non-table heads/git_subcommands, non-string entries, and empty-string entries, each naming the offending field (e.g. 'bash_allow.heads')"]
  notes = "Every malformed-shape branch named in the criterion (wrong container type, non-string entry) has its own assertion, each checking the error names the specific bad field."

[[criterion]]
  id = "c-4"
  status = "uncovered"
  notes = "Purely a documentation criterion: the README's Options section reads as one coherent reference for auto_start/claudecode/test_command/bash_allow (confirmed by inspection at README.md:59-98), but no test reads README.md, so nothing catches this section drifting out of sync with the actual config surface."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["tests/nvim/health_check.lua (Bash allowlist heads line lists hardcoded defaults with no bash_allow configured; heads/git-subcommand lines include bash_allow.heads/git_subcommands additions after a second setup())"]
  notes = "Drives the real :checkhealth codriver command (not health.check() directly) and asserts both the cold default listing and the additions after setup() with bash_allow configured."

[[finding]]
  severity = "BLOCKING"
  text = "criterion c-2 (test_command's README documentation) has no covering test — README.md is not read by any test in the suite"

[[finding]]
  severity = "BLOCKING"
  text = "criterion c-4 (Options section as one coherent reference) has no covering test — README.md is not read by any test in the suite"

[[finding]]
  severity = "NOTE"
  text = "c-1's README-documentation clause shares the same gap as c-2/c-4 (no test reads README.md), but is not itself blocking since the criterion's behavioural substance is thoroughly covered"

[[finding]]
  severity = "NOTE"
  text = "mutation skipped: no adapter for .lua/.md — verdict is based on criterion coverage alone, per the unmeasurable/skipped mutation-status rule"

[[finding]]
  severity = "NOTE"
  text = "skipped README.md: no mutation adapter for .md"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/config.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/health.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/hook/bash.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/hook/decision.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/hook/state.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped lua/codriver/init.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/codriver/config_spec.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/codriver/hook_bash_spec.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/codriver/hook_decision_spec.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/nvim/health_check.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/nvim/hook_publish_check.lua: no mutation adapter for .lua"

[[finding]]
  severity = "NOTE"
  text = "skipped tests/nvim/hook_state_check.lua: no mutation adapter for .lua"
